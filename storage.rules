rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Authenticated users can read/write only their own expense_manager folder (e.g. profile pics)
    match /expense_manager/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
